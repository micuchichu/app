import React from 'react';
import { Text, View } from 'react-native';

export const JobsMapModal = (_props: any) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Map view is not available on web.</Text>
    </View>
  );
};